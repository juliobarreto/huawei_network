{% for rule in acl_rules | default([]) %}
acl {{ rule.acl_number | default('') }}
 {% for entry in rule.entries | default([]) %}
  rule {{ entry.rule_id | default('') }} {{ entry.action | default('') }} {% if entry.protocol is defined %}protocol {{ entry.protocol }}{% endif %} source {{ entry.source | default('') }} {% if entry.destination is defined %}destination {{ entry.destination }}{% endif %}
 {% endfor %}
quit
{% endfor %}